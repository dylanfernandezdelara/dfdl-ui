# Decisions

Every value Dylan approved, with the alternatives it was chosen against and one line of why. Append in the same commit as the change. The `dfdl-ui` skill is written from this file, so an entry here is the source; the skill restates it.

Format: `date · area · decision · chosen against · why · status`

Status is `approved`, `interim` (in use but not yet reviewed in its own lab), or `superseded` (with a pointer to the newer entry).

## Foundations

| Date | Area | Decision | Chosen against | Why | Status |
| --- | --- | --- | --- | --- | --- |
| 2026-09-15 | Color model | OKLCH for all tokens, sRGB hex fallbacks generated, two tiers (primitives by hue and step, semantics by role) | HSL, hand-picked hex | Perceptual uniformity makes matched-lightness comparison and predictable ramps possible; matches dylanfdl.com's existing approach | approved |
| 2026-09-15 | Token naming | Semantics `--bg-*`, `--fg-*`, `--border-*`, `--accent-*`, `--status-*`, `--rounded-*`, `--motion-*`, `--elevation-*`; utilities `text-fg`, `border-line`, `bg-accent-solid`. `fg` is the one word for foreground (Jakub’s one-word rule; his own word is `text`, which collides with Tailwind’s `text-*` size utilities here); `primary` is reserved for shadcn compatibility and never means body text | `--color-text-primary` (collides with shadcn `primary` and with Tailwind theme keys) | Jakub's one-word rule; Tailwind theme keys must differ from source names or they compile self-referentially | approved |
| 2026-09-19 | Neutral | Unified warm: one hue (80) across surfaces and text, chroma 0.014, tint held across the light steps. Text lightness structure kept from dylanfdl.com (body 37%, secondary 55%, tertiary 71%, strong 12%; dark 88.5/68/58/93.5; light tertiary superseded 2026-09-25, see Tertiary text) | A: cream surfaces with cool slate text (dylanfdl.com), C: near neutral (chroma 0.003) | Dylan’s pick in Lab 1 (`/lab/color?n=b`). Surfaces and text now sit in the same family; the cool text was the one thing that read as two systems | approved |
| 2026-09-19 | Accent | Ember, hue 48, at the previous indigo’s solid lightness (52.4% light, 72% dark) and 0.452 relative vividness | A: indigo, C: moss | Dylan’s Lab 1 pick | superseded by 2026-09-24 accent set |
| 2026-09-24 | Accent set | Two approved accents, chosen once per product with `data-accent` on the root: **ember** (hue 48, default; vividness 0.60 of ceiling light, 0.55 dark) and **indigo** (hue 281, Fork; 0.452 light, 0.80 dark, the value dylanfdl.com already shipped). Same solid lightness, so they are interchangeable. Dark vividness is set per accent because blue’s sRGB chroma ceiling halves between L 52% and L 72% and one factor cannot serve every hue | one accent for every product; deriving dark from light with one factor | Dylan wants Fork to stay blue while other apps use ember. Identity lives in the neutral, type, rhythm, radius, elevation and motion; the accent is the one dial a product turns. Radix Themes uses the same model. Ember vividness raised from 0.452 to 0.60 after the 1b boards: at 0.452 the send button sat too close to the neutrals to read as an action | approved |
| 2026-09-19 | Depth | Ring and lift kept: light `0 0 0 1px black/6%, 0 1px 2px -1px black/6%, 0 2px 4px black/4%`; dark `inset 0 0 0 1px white/4%, 0 1px 1px black/24%`; floating adds a 10px/15px drop (light) or 16px/40px (dark) | B: hairline only, C: lit edge (dialkit inset highlight) | Dylan left A selected in Lab 1. Jakub’s light recipe and gooey’s dark recipe, measured from the audit. The floating drops are ours: light matches kitze’s `shadow-lg`, the dark 16px/40px was not measured anywhere | approved |
| 2026-09-19 | Status hues | danger 18, success 150, warning 88; each a 12-step ramp with bg (3), border (6), solid (9), text (11) | dylanfdl.com’s red 21 / yellow 66 / green 145 | Warning at 66 sat only 18 degrees from the new accent, 3 degrees of margin over better-colors’ 15-degree rule. All three now 30 degrees or more from the accent, text steps measure 8:1 or better on the page | approved |
| 2026-09-19 | Color source | Color tokens are generated by `scripts/color/build-tokens.mts` from a config block; `tokens.css` carries GENERATED markers. Hand edits to color values are not allowed | hand-authored values | Reproducible ramps, measured contrast in the build output, and the same source will feed Figma variables in Phase 6 | approved |
| 2026-09-24 | Type scale | Weight-led: body, heading and title share one size; hierarchy from weight 550, family and color. Display 32/40 at 400, tracking -0.01em. UI 13px/20px at 450 for dense surfaces, caption 12px/16px at 450 | A: previous dylanfdl.com scale (16 body, 14 UI, one heading size); B: Jakub’s size-led role scale (36/24/18/16/13, with a 14px UI row added by us) | Dylan’s pick in Lab 2 (`?s=c`). The structure is jakub.kr’s (one size, weight 550 headings). The lab set the size at 15px; that number was my synthesis and I wrongly attributed it to jakub.kr and benji.org (jakub.kr body is 16/26; benji.org paragraphs are 14/20 at 460 in a dense catalogue). Corrected 2026-09-25 | superseded, see Body size |
| 2026-09-25 | Body size | Body, heading and title at 16px/24px. UI 13/20 and caption 12/16 unchanged | 15px (the Lab 2 value) | The dylanfdl.com preview read too small at 15 on the homepage blurb; 16 is what the site shipped and what jakub.kr measures. Weight-led hierarchy is unchanged. Provenance error above prompted a full audit of attributed claims | superseded, see Type sizes (measured). "What the site shipped" was false: it shipped 14 |
| 2026-09-25 | Type sizes (measured) | Sizes taken from dylanfdl.com's rendered text before the dfdl merge: `body` 14/24 at 450 (page prose, the default), new `reading` 15/24 at 400 (long-form articles), `display` Lora 24/32 at 400 (page title). Weight-led structure kept: `heading` 14/24 and `title` 15/24 at 550 in Lora, each the size of the text it heads. UI 13/20 and caption 12/16 unchanged | 16/24 body (the previous row); 15 everywhere (Lab 2) | Dylan: after the merge the homepage prose was too big. Root cause: the Phase R audit summarized dylanfdl.com from the `<body>` element's 16px default while its paragraphs render 14/24 at 450 (the raw data in `research/inspo/computed-styles.json` had it right). That wrong 16 became the Lab 2 "Previous" candidate, the "site shipped 16" claim, and the 16px blurb. The Lab 2 15px was not a synthesis either: it is the site's article body size. Rule added: measure rendered paragraphs, never `<body>` | superseded, see Type structure |
| 2026-09-25 | Type structure | Size-led, as dylanfdl.com ships it: Lora only on titles (`headline` 28/32 for articles, `display` 24/32 for pages, weight 400); headings sans at 600 stepping down by size (`title` 20/32, `heading` 17/24); `body` 14/24 at 450, `reading` 15/24, `ui` 14/20 at 450, `caption` 12/16 | Lab 2's weight-led structure with Lora on every heading; UI 13 | Dylan's pick once the measured site was on the table: the goal is that a product built from dfdl comes out like dylanfdl.com. Lab 2's choice was made against a baseline that misdescribed the site (16/24 body) | approved |
| 2026-09-25 | Tailwind version | dfdl is Tailwind v4 only. A v3 site adopts tokens as is (tokens.hsl.css); to adopt components it upgrades to v4 in its adoption PR (Tailwind's upgrade tool, before/after screenshots, its tests) | a v3 preset with v3/v4-compatible component syntax; tokens only on v3 sites | Dylan's pick. One theme definition and no syntax restriction on every future component; the migration cost is paid once per site | approved |
| 2026-09-25 | Tailwind v4 upgrades | Run `@tailwindcss/upgrade` for the config and class renames, but keep the site's component CSS as plain CSS in `@layer components`; do not accept its conversion of component classes to `@utility`. Prove "no visual change" by pixel-diffing full-page screenshots and interactive states against the v3 build | accept the tool's output | On trackcongress the tool turned ~400 component classes into utilities, which v4 emits only when its scanner finds the name; classes built in template literals vanished (the header flag grew to 1075px, the chamber filter lost its styles). The pixel diff also found latent v3 bugs the upgrade fixes | approved |
| 2026-09-25 | Inline baseline | A box inside running text (animated word, badge, kbd) aligns with `vertical-align: baseline` and never has non-visible overflow; mask it with `clip-path` instead. Text alignment is verified in WebKit as well as Chromium. `scripts/probes/baseline.js` measures it and half a pixel counts | `text-bottom` (what dylanfdl.com's slot-text cells used); `baseline` with `overflow-y: clip` (the first fix) | The "optimist." word sat 0.5px high at 14px and 1px high at 16px: a 1.25em cell aligned text-bottom leaves an offset proportional to font size. The first fix measured 0.00 in Chromium but was 3.5px high in Safari, because WebKit takes the baseline of an inline-block with hidden or clip overflow from its bottom edge. Dylan caught it in the preview. With clip-path: 0.00 in Chromium, WebKit and Firefox at 13 to 20px | approved |
| 2026-09-24 | Serif scope | Lora on every heading level and the display, including section headers inside app panels. Body and UI stay system sans | A: display only (previous); C: none | Dylan’s pick (`?f=b`). Serif headings carry the editorial identity into apps; with weight-led sizing the family change is what separates a heading from body | superseded, see Type structure (titles only) |
| 2026-09-24 | Radius | Round: xs 8 · sm 10 · md 12 · lg 16 · xl 24 · full. Concentric nesting is the rule: outer = inner + padding. Controls at sm, fields at md, cards at lg, sheets at xl | A: previous 4/6/8/12/16; B: soft 6/8/10/14/20 (Fork’s derived shadcn scale, not kitze’s as first labeled) | Dylan’s pick (`?r=c`). These are values Fork already uses literally (8, 10, 12, 24 appear in its CSS), though Fork’s derived `--radius-*` scale is 6/8/10/14 and would change | approved |
| 2026-09-24 | Type roles as tokens | Type is set only through role utilities `text-display`, `text-title`, `text-heading`, `text-body`, `text-ui`, `text-caption`, each carrying size, leading and weight; families via `font-display` and `font-heading`. Ad hoc `text-sm leading-6` combinations are not allowed | per-property Tailwind classes | One place to change a role; the linter and `cn` know the vocabulary; Figma text styles map one to one | approved |
| 2026-09-24 | Quiet mode | `<html data-quiet>` for data-dense products: titles go system sans (headings already are), the accent roles collapse into the neutrals (selection carried by weight and hairlines, focus ring is `fg-strong`). Neutrals, type roles and sizes, radius, elevation and motion are unchanged. trackcongress runs in quiet mode | apply Lora headings and ember everywhere; or exempt trackcongress from dfdl entirely | Phase 5 trackcongress before/after. Serif headlines on a stacked feed read as two registers per row, and a fifth color on a page where green, red, amber and party colors already carry meaning asks the reader to learn what orange means. Tool surfaces are generally quieter than reading surfaces; not measured in the audit. Warm charcoal dark and the 16px heading role stand in quiet mode | approved |
| 2026-09-15 | Grid | 8px minor, 24px major rhythm with overlay | none considered | Already proven on dylanfdl.com; overlay ported as is | approved |
| 2026-09-24 | Grid alignment, enforced | Every block's top edge and every control's height land on the 8px grid; the 4px half-step is allowed only for centering a 24px line in a 48px row and for padding inside a control; nothing fractional. Line-heights are fixed pixel multiples of 4, never unitless. A 1px border that would push content off the grid becomes a `hairline`, `hairline-t` or `hairline-b` shadow (no layout space). Verification is mechanical: `Shift+G` in the browser outlines every offender with its reason; `scripts/probes/grid.js` returns the same list for agents. A component page ships at zero strict offenders | eyeballing against the overlay | Dylan: slight misalignments ruin an otherwise crafted layout, and they compound: one 2px error in a section header shifted every element below it on the type lab. Measured, not judged | approved |
| 2026-09-24 | Inline runs and the line box | Inline elements inside text blocks (`em`, `i`, `strong`, `b`, `code`, `kbd`, `sup`, `sub`) get `line-height: 1`, and `sup`/`sub` are positioned rather than shifted, so a serif emphasis or a padded code span cannot inflate a line from 24px to 25px | leave browser defaults | Found by the grid probe: one `<em class="font-display">` in a paragraph pushed the rest of the article a pixel off the grid | approved |
| 2026-09-25 | Attributions | A value credited to a site or person cites where it was measured (`research/inspiration.md`, `research/inspo/`) or published (`.agents/skills/`). A value we chose ourselves says so. “The audit found” is only written about what the audit found | attribute from memory | A three-agent audit after the 15px error found eleven more: exits “a quarter shorter” (the audit found no site shortens them), “every site uses a strong ease-out” (two do), 0.94 “kitze-like” (kitze has no press scale), 6/8/10/14/20 as “kitze’s family” (it is Fork’s derived scale), Fork’s radius “needs no change” (its tokens would), and smaller ones. All corrected in place | approved |
| 2026-09-25 | Focus ring | Every focus stop draws `2px solid var(--border-focus)` at a 2px offset, from a zero-specificity base rule; components may restyle it but never remove it | per-component `focus-visible` classes only | The audit found links, menu items, tabs and specimen controls with no visible focus at all; the base layer only set a color on an outline that was never drawn | approved |
| 2026-09-25 | Tertiary text | Light `fg-tertiary` at 62% lightness (was 71%): 3.56:1 on the page, 3.2:1 on sunken. Dark unchanged at 58% (4.4:1). Tertiary never goes below 3:1 on any surface it sits on | keep 71% (2.48:1); 65% (3.16 on page but 2.84 on sunken) | The audit found 71% failed the color lab’s own 3:1 floor, and tertiary carries real content (labels, dates, captions, placeholders). 62% is the lightest value that clears 3:1 on sunken fields and stays a clear step from secondary (4.73:1) | approved |
| 2026-09-25 | Control height | Default control (`h-control`) is 32px; 28 is the dense size (chips, table actions); the scale stays 24/28/32/40/48 | 28px default, approved in Lab 2 and dropped from the type-scale row on 2026-09-25 without a record | Centered in the 40 and 48 rows the layout uses, 32 lands at 4 and 8 and stays on the grid; 28 lands at 6 and 10 and does not (the four `/lab/motion` offenders). The segmented controls and the docs already used 32. Supersedes Lab 2’s 28px default | approved |
| 2026-09-25 | Docs site presentation | The docs site reads as a component library: sans throughout its chrome (headings included), one-line descriptions, no mono or uppercase labels, component pages with Preview/Code, install command, usage, examples and props, an "On this page" column. Component previews and type specimens keep the product default (Lora headings) via `data-product-type` | Lora headings and mono eyebrows in the chrome; long explanatory ledes | Dylan: keep fonts simple, the mono tagline read as AI smell, text too verbose; references benji.org/liveline, docs.once-ui.com, elements.ai-sdk.dev | approved |
| 2026-09-25 | Grid audit scope | The probe skips out-of-flow decoration (`aria-hidden`, absolutely positioned) and the interior of preview frames (`data-grid-ignore`), where demos of arbitrary height are centered. Component control heights stay on the scale | audit everything | A centered 20px demo cannot land on the page grid in any frame height; the tab indicator is a 2px bar that shifts nothing | approved |
| 2026-09-25 | Control contracts | Every control (Button, Input, SegmentedControl, Switch, TabsTab, TabsList, MenuItem, Badge, Kbd) denies `h-*`, `min-h-*`, `max-h-*` and `size-*` from callers; Switch also denies width. Layout otherwise stays open (width, margin, flex) | the default `allow: ["layout"]` alone | Tested: the default lets `h-12` and `w-48` through on a Button, which breaks the 24/28/32/40/48 scale silently. Pick a size instead | approved |
| 2026-09-25 | Skill shape | `skills/dfdl-ui/SKILL.md` leads with the workflow (check setup, use a registry component, verify with lint and probes), then values, then the judgment the linter cannot check. Mechanical rules stay in the linter; craft beyond dfdl is handed to the vendored skills by name | a values reference first; restating Emil and Jakub | An agent's first mistake is hand-building what exists; its second is not measuring. Values are one lookup away in llms.txt | approved |
| 2026-09-24 | Docs site as first consumer | The docs site (ui.dylanfdl.com) is built with `@dfdl/*` components and nothing ad hoc. Component order follows what the site needs: Button, Segmented control, Card, Command (copy block), Kbd, Table, Swatch, then Tabs, Menu, Dialog, Drawer. Structure borrows from ui.kitze.io (sidebar, overview, per-component pages, llms.txt); the voice and every visual is ours | build the site with throwaway markup and add components later | Dylan, 2026-09-24: the site should use the components we designed. Satisfies the consumer rule and turns the site into the standing before/after: if a component is wrong, the docs look wrong | approved |
| 2026-09-15 | Type families | System sans body, Lora display and emphasis, Geist Mono code and metadata | Geist everywhere, Inter + serif | Continuity with dylanfdl.com; zero font cost for body | approved |
| 2026-09-15 | Motion, interim | `cubic-bezier(0.23, 1, 0.32, 1)`, 150ms hover, `scale(0.97)` press | none yet | Emil's defaults so labs 1 and 2 do not feel broken before the motion lab | superseded by 2026-09-24 motion |
| 2026-09-24 | Motion curves | Enter and exit `cubic-bezier(0.23, 1, 0.32, 1)`; on-screen movement (tabs, reorder) `cubic-bezier(0.77, 0, 0.175, 1)`; drawers `cubic-bezier(0.32, 0.72, 0, 1)`; a spring `linear()` (Emil’s Apple-style config with bounce lowered from 0.2 to 0.15) reserved for gesture-driven motion only. Never `ease-in`, never built-in `ease` on deliberate motion | B: Fork’s built-in `ease`; C: Material decelerate `(0, 0, 0.2, 1)` | Dylan accepted the recommendation in Lab 3 after synchronized ¼-speed comparison. Strong ease-out puts the distance in the first third so the element is where it is going by the time the eye lands; `ease` reads as lag. gooey and beautifului measure near-identical strong ease-outs; kitze uses Tailwind's milder curves and jakub the `ease-out` keyword | approved |
| 2026-09-24 | Durations by frequency | instant 100ms (tooltips, things seen tens of times a day), fast 150 (hover, press, switches), normal 200 (menus, popovers, toasts), slow 300 (dialogs and backdrops), slower 500 (drawers). Nothing UI over 500ms. Keyboard-triggered and 100+/day actions do not animate at all | one duration for everything | Emil’s frequency table; the audit measured 78–150ms hover, 150 menus, 200 dialogs, 300 drawers on the reference sites | approved |
| 2026-09-24 | Exits | Exits run at 75% of the enter duration along the same path (menu closes into its trigger, toast leaves the way it came). Toasts and reduced-motion exits fade only | A: symmetric; C: 50% fade-only for everything | Accepted recommendation. The user has already decided when something leaves; the audit found no reference site shortens its exits; the 25% comes from Emil's and Jakub's published recommendation | approved |
| 2026-09-24 | Press | `scale(0.97)` at 150ms on every pressable; `:active` only, no hover gating needed. Dropped under `prefers-reduced-motion`; color still changes | B: none; C: 0.94 | Accepted recommendation. Smallest scale that still registers as feedback; 0.94 is visible on icon buttons | approved |
| 2026-09-24 | Motion mechanics | Popovers, menus and tooltips scale from `0.95` (tooltips `0.97`) at the trigger’s transform-origin; dialogs from `0.96` at center; drawers translate by their own height; toasts translate 16px (a narrowing of Emil’s recipe, which slides the full height at 400ms). Only `transform`/`translate`/`scale` and `opacity` animate; tab indicators move with `translate`, never `left`/`width`. Transitions, not keyframes, for anything a user can fire twice. Enters start in `@starting-style`, exits in the closed state, `display` flips with `allow-discrete`, so enter and exit can differ. Reduced motion keeps opacity and color and drops movement and scale | JS-driven animations; `transition: all` | Emil’s rules, verified by `getAnimations()` on every lab specimen | approved |

## Architecture

| Date | Area | Decision | Chosen against | Why | Status |
| --- | --- | --- | --- | --- | --- |
| 2026-09-15 | Primitives | Base UI + Tailwind v4 | Radix, plain CSS | Where shadcn and Kitze moved; Fork already uses it | approved |
| 2026-09-15 | Motion library | CSS-first; `motion` only in expressive and AI components that need springs | motion everywhere, CSS only | Core stays dependency-free; per-item registry deps keep heavy pieces out of `button` | approved |
| 2026-09-15 | Distribution | shadcn registry, `@dfdl` namespace, `registry:base` preset | npm package | Source lands in the consumer project; adoption of outside components is copy and retoken | approved |
| 2026-09-15 | Agent guidance | One `dfdl-ui` skill that hands off to Emil, Jakub and Figma skills by name | Google `DESIGN.md`, copying reference skills | Skills are the spec with a validator and measured effect; a values file alone moved nothing in any published test | approved |
| 2026-09-15 | Enforcement | `@shadcn/lint`: no raw colors, no arbitrary values, per-component contracts | prose rules only | Mechanical rules belong in a linter; agents reach zero violations in one round in shadcn's evals | approved |
| 2026-09-15 | Figma to code mapping | `dfdl/X` component naming plus variable code syntax plus descriptions, in place of Code Connect | Organization plan for Code Connect | Code Connect is Organization-only; the convention is what `get_design_context` reads below Code Connect anyway | approved |
| 2026-09-15 | Repo shape | Single Next.js app: docs site, component source, registry build | monorepo | Kitze and AI Elements shape; split later only for an npm or non-web target | approved |
| 2026-09-15 | Hosting | Cloudflare, deferred | Vercel | Dylan's current platform; nothing deployed until asked | approved |
